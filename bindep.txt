# System packages needed when building the EE locally via ansible-builder
# (kept as a reference; the Dockerfile handles these with microdnf directly)
gcc            [platform:rpm]
python3-devel  [platform:rpm]
krb5-devel     [platform:rpm]
openssl-devel  [platform:rpm]
git            [platform:rpm]
