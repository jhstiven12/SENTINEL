---
# =============================================================================
# playbook_rhel.yml — SENTINEL para RHEL 7 / 8 / 9 / 10
# =============================================================================

- name: SENTINEL — Descubrimiento de servicios y procesos Java en RHEL
  hosts: rhel_servers
  gather_facts: true
  become: true
  roles:
    - role: svc_disc_rhel
